# Dockerfile for Netquery Server Adapter (Python)
FROM python:3.11-slim

# Set working directory
WORKDIR /app

# Copy requirements and adapter script
COPY requirements.txt .
COPY netquery_server.py .

# Install Python dependencies
RUN pip install --no-cache-dir -r requirements.txt

# Expose the port (update if adapter uses different port)
EXPOSE 3001

# Start the adapter server
CMD ["python", "netquery_server.py"]